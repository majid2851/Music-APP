package com.musicapk.presentation.songs.model

import com.musicapk.ui.theme.Strings

enum class MainTabEnum(val title: String) {
    SONGS(Strings.songs),
    FAVORITES(Strings.favorites),
    PLAYLISTS(Strings.playlists),
//    ARTISTS(Strings.artists),
//    ALBUMS(Strings.albums),
}

