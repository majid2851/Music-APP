package com.musicapk.presentation.songs.model

import com.musicapk.ui.theme.Strings

enum class SongCategoryEnum(val title: String) {
    ALL_MUSIC(Strings.allMusic),
    RECENT(Strings.recent),
    TOP_50(Strings.top50),
    CHILL(Strings.chill),
    RNB(Strings.rnb),
    FESTIVAL(Strings.festival)
}



